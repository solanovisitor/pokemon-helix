# Continue development

Use this prompt for the next bounded implementation. Read the current
[verification record](verification.md), [native source limits](../rom-source/README.md),
[quest design](quest-agents.md), [agent tools](agent-tools.md) and
[publication workflow](publication.md) first. Their recorded evidence takes
precedence over assumptions about an available package or device.

> Continue Pokémon Helix in this public checkout. Deliver a concrete, playable,
> tested and documented increment; do not stop at research or a proposal.
>
> Implement one small quest thread that continues coherently across NPC contact,
> a player decision and a saved follow-up. Add a bounded `ask_user_question`-style
> capability: one question, a small fixed choice menu, stable choice IDs and
> visible labels of two or three words. Use native button input, with no free-text
> entry. Make cancellation and returning later understandable to the player.
>
> First establish the runnable boundary. If the public checkout still lacks its
> required gameplay package, prepare the smallest complete synthetic public
> fixture needed for this path. Give it its own identities and save namespace.
> Never import accepted individuals, private stores or personal saves to make a
> demonstration work. Preserve upstream history, notices and public sanitizations.
>
> Keep planning and inference outside the emulator frame loop. Give the director
> only an approved public dossier and bounded tools. Validate the entire proposal
> before presenting it. Native code must recheck quest revision, eligibility and
> costs before applying an effect. Preserve pending, rejected and ready semantics;
> ready must describe the checks actually completed. Default to deterministic
> fixtures. Any live use needs explicit configuration, a bounded request budget
> and verified provider capabilities; do not assume a model or credential exists.
>
> Reuse validated public definitions and cached art where appropriate. Bind cache
> entries to producer and policy identity, retain provenance, and keep private
> quest instances, ownership and individual IDs separate. Replay must not repeat
> a charge, create another individual or grant another reward. A curator may
> admit reusable definitions only through an explicit reviewed boundary.
>
> Preserve V14 as the accepted local default. V15c, V16b, V17c and V18b remain
> candidates unless a later explicit acceptance record says otherwise. Preserve
> earlier source snapshots, builds, evidence and saves. CORAL stays blocked.
>
> Use parallel agents for bounded design/research, gameplay and independent
> verification. Centralize integration, compilation and mGBA operation. Choose
> one small deliverable and finish it before expanding scope.
>
> Prove the exact compiled candidate in mGBA: inspect screenshots and assert
> native state for the complete thread, cancellation, invalid/stale replies,
> duplicate delivery, offline fallback and ordinary Save/Continue. Use isolated
> synthetic data and release injected inputs. Record source/toolchain/ROM hashes
> and reproducible commands; do not publish ROMs, credentials or saves.
>
> Consider RG34XX controls, screen space, offline behavior and save handling from
> the start. Report host tests, Linux ARM64 Docker, desktop mGBA and physical
> RG34XX validation separately. Never substitute one for another.
>
> Document what changed, what is actually playable, reproduction steps and
> remaining limits. Keep player-facing text concrete and preserve language
> selection. Do not claim autonomous world generation, runtime content loading,
> shared-world authority or a complete public release from a smaller proof.

Publishing the resulting source is a separate repository action governed by
the current request and publication workflow. Keep historical evidence attached
to its original build rather than relabeling it as proof of the new candidate.
